# Dependencies
node_modules/
**/node_modules/

# Lockfiles — kept untracked because they are OS-specific for native
# binaries like lightningcss/sharp; EAS regenerates on its build OS.
app/package-lock.json

# Environment & secrets (keep .env.example — it has no real values)
.env
.env.local
.env.production
.env.development
**/.env
**/.env.local
**/.env.production
**/.env.development
**/functions/.env.*
**/sa-key.json
**/credentials.yml

# Build outputs
.expo/
dist/
.next/
out/
build/
android/
ios/

# Firebase
**/.firebase/
**/functions/.env.*
# Firebase mobile config — already in history (e2c0c5b), but block future adds.
# Web API keys are "public by design" per Firebase docs; real security relies
# on Firestore Rules + App Check + GCP API key restrictions.
google-services.json
**/google-services.json
GoogleService-Info.plist
**/GoogleService-Info.plist

# Misc
.DS_Store
*.log
.vercel

# Android signing — NEVER commit these
*.jks
*.keystore
credentials.json
gradle.properties.local

# Relatórios gerados localmente (contêm PII: nomes/CPFs/PIX)
relatorio-*.csv
**/relatorio-*.csv
